name: Manual Release Creation

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (e.g., v1.0.0)"
        required: true
        type: string
      tenant:
        description: "Select Tenant"
        required: true
        type: choice
        options:
          - inter
          - ingka
      environment:
        description: "Select Environment"
        required: true
        type: choice
        options:
          - dev
          - stage
          - prod
      release_type:
        description: "Release Type"
        required: true
        type: choice
        options:
          - prerelease
          - release

permissions:
  contents: write
  deployments: write

jobs:
  validate:
    name: "Validate Inputs"
    runs-on: ubuntu-latest
    outputs:
      full_version: ${{ steps.calculate-version.outputs.full_version }}
    steps:
      - name: Calculate Full Version with Tenant
        id: calculate-version
        run: |
          VERSION="${{ inputs.version }}"
          TENANT="${{ inputs.tenant }}"
          
          # Remove 'v' prefix if present
          VERSION=${VERSION#v}
          
          # Create tenant-prefixed version
          FULL_VERSION="${TENANT}-v${VERSION}"
          
          echo "full_version=$FULL_VERSION" >> $GITHUB_OUTPUT
          echo "📦 Full version tag: $FULL_VERSION"
      
      - name: Validate inputs
        run: |
          TENANT="${{ inputs.tenant }}"
          ENV="${{ inputs.environment }}"
          VERSION="${{ inputs.version }}"
          RELEASE_TYPE="${{ inputs.release_type }}"
          
          echo "📋 Validation Starting..."
          
          # Check all inputs are provided
          if [[ -z "$VERSION" ]]; then
            echo "❌ Error: 'version' is required"
            exit 1
          fi
          
          if [[ -z "$TENANT" ]]; then
            echo "❌ Error: 'tenant' is required"
            exit 1
          fi
          
          if [[ -z "$ENV" ]]; then
            echo "❌ Error: 'environment' is required"
            exit 1
          fi
          
          # Business rules
          if [[ "$TENANT" == "inter" && "$ENV" == "dev" ]]; then
            echo "❌ Error: 'inter' tenant cannot be deployed to 'dev' environment"
            exit 1
          fi
          
          if [[ "$RELEASE_TYPE" == "release" && "$ENV" != "prod" ]]; then
            echo "⚠️  Warning: Creating full release for non-prod environment"
          fi
          
          echo "✅ All validations passed!"
          echo ""
          echo "📦 Release Details:"
          echo "  - Version: $VERSION"
          echo "  - Tenant: $TENANT"
          echo "  - Environment: $ENV"
          echo "  - Type: $RELEASE_TYPE"

  create-release:
    name: "Create ${{ inputs.release_type }}: ${{ needs.validate.outputs.full_version }}"
    runs-on: ubuntu-latest
    needs: [validate]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Check if tag exists
        id: check-tag
        run: |
          FULL_VERSION="${{ needs.validate.outputs.full_version }}"
          if git rev-parse "$FULL_VERSION" >/dev/null 2>&1; then
            echo "tag_exists=true" >> $GITHUB_OUTPUT
            echo "⚠️  Tag $FULL_VERSION already exists"
          else
            echo "tag_exists=false" >> $GITHUB_OUTPUT
            echo "✅ Tag $FULL_VERSION is available"
          fi
      
      - name: Create Tag (if needed)
        if: steps.check-tag.outputs.tag_exists == 'false'
        run: |
          FULL_VERSION="${{ needs.validate.outputs.full_version }}"
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git tag -a "$FULL_VERSION" -m "Release $FULL_VERSION for ${{ inputs.tenant }}-${{ inputs.environment }}"
          git push origin "$FULL_VERSION"
          
          echo "✅ Created tag: $FULL_VERSION"
      
      - name: Generate Release Notes
        id: release-notes
        run: |
          FULL_VERSION="${{ needs.validate.outputs.full_version }}"
          
          NOTES=$(cat <<EOF
          ## 🚀 Deployment Information
          
          This ${{ inputs.release_type }} is for **${{ inputs.tenant }}-${{ inputs.environment }}** environment.
          
          ### 📋 Details
          - **🏢 Tenant:** \`${{ inputs.tenant }}\`
          - **🌍 Environment:** \`${{ inputs.environment }}\`
          - **📦 Version:** \`${{ inputs.version }}\`
          - **🔖 Full Tag:** \`$FULL_VERSION\`
          - **📑 Type:** \`${{ inputs.release_type }}\`
          - **📅 Created:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          - **👤 Triggered by:** @${{ github.actor }}
          
          ### 🔗 Links
          - [Workflow Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          - [Commit](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
          
          ---
          
          _Automated release created via GitHub Actions_
          EOF
          )
          
          echo "$NOTES" > release_notes.md
          
          echo "### 📝 Release Notes Preview" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "$NOTES" >> $GITHUB_STEP_SUMMARY
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate.outputs.full_version }}
          name: "[${{ inputs.tenant }}] ${{ inputs.version }} - ${{ inputs.environment }}"
          body_path: release_notes.md
          prerelease: ${{ inputs.release_type == 'prerelease' }}
          draft: false
          make_latest: false
      
      - name: Create Deployment Record
        uses: chrnorm/deployment-action@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          environment: ${{ inputs.tenant }}-${{ inputs.environment }}
          ref: ${{ needs.validate.outputs.full_version }}
          initial-status: success
          description: "Released ${{ needs.validate.outputs.full_version }} to ${{ inputs.tenant }}-${{ inputs.environment }}"
      
      - name: Summary
        run: |
          FULL_VERSION="${{ needs.validate.outputs.full_version }}"
          
          echo "### ✅ Release Created Successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Full Tag:** \`$FULL_VERSION\`" >> $GITHUB_STEP_SUMMARY
          echo "**Type:** \`${{ inputs.release_type }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Target:** \`${{ inputs.tenant }}-${{ inputs.environment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [View Release](${{ github.server_url }}/${{ github.repository }}/releases/tag/$FULL_VERSION)" >> $GITHUB_STEP_SUMMARY
